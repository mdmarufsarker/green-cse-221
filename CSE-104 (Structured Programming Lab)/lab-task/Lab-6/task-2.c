#include<stdio.h>

int main(){
    int n, arr[100], i, sum = 0, count = 0;
    float avg = 0;

    scanf("%d", &n);

    for(i = 0; i < n; i++){
        scanf("%d", &arr[i]);
    }

    for(int j = 0; j < n; j++){
        if(arr[j] )
    }

    return 0;
}